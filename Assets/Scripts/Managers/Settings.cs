using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Settings : MonoBehaviour
{
    // These variables can be accessed without being attached to a Game Object because they are static, you can get or set any variable with "Settings.variableName;"
    public static float masterVolume;
}
